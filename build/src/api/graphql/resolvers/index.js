"use strict";
/*import { Container } from 'typedi';

import { IUser } from '../../../interfaces/IUser';
import { getBaseRepository } from 'fireorm';

export const graphQlResolvers = {
  users: async () => {
      
    const UserModel = Container.get('userModel') as IUser//mongoose.Model<IUser & mongoose.Document>;
    const usersRepositoty =  getBaseRepository(Container.get('userModel'));
    return await usersRepositoty.findById('1');
  },
};
*/ 
//# sourceMappingURL=index.js.map